{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'No build step for Python API'",
    "lint": "uv run ruff check .",
    "lint:fix": "command -v uv >/dev/null 2>&1 && uv run ruff check --fix . || (echo 'api: skipping lint:fix (uv not installed)' && exit 0)",
    "format": "uv run ruff format .",
    "format:check": "uv run ruff format --check .",
    "check-types": "echo 'No typecheck for Python'"
  }
}
