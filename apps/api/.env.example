# Database
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/renly

# JWT — must match the web app's NEXTAUTH_SECRET; minimum 32 characters (validated at startup).
JWT_SECRET=
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080 # 7 days
